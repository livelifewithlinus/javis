const state=new Map<string,{messages:Array<{role:string;content:string}>}>(); export function getWorkspaceState(workspaceId="default"){if(!state.has(workspaceId))state.set(workspaceId,{messages:[]});return state.get(workspaceId)!} export function addWorkspaceMessage(workspaceId:string,role:string,content:string){const workspace=getWorkspaceState(workspaceId);workspace.messages.push({role,content});return workspace.messages}
