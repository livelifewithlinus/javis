import {z} from "zod";
export const envSchema=z.object({OPENAI_API_KEY:z.string().optional(),OPENAI_MODEL:z.string().default("gpt-5.6-terra"),DATABASE_URL:z.string().default("postgres://jarvis:jarvis@localhost:5432/jarvis"),REDIS_URL:z.string().default("redis://localhost:6379"),APP_URL:z.string().default("http://localhost:3000"),JARVIS_DEV_USER_ID:z.string().default("dev-user"),JARVIS_DEV_WORKSPACE_ID:z.string().default("dev-workspace"),COMPANION_PAIRING_SECRET:z.string().default("development-only")});
export const env=envSchema.parse({OPENAI_API_KEY:process.env.OPENAI_API_KEY,OPENAI_MODEL:process.env.OPENAI_MODEL,DATABASE_URL:process.env.DATABASE_URL,REDIS_URL:process.env.REDIS_URL,APP_URL:process.env.APP_URL,JARVIS_DEV_USER_ID:process.env.JARVIS_DEV_USER_ID,JARVIS_DEV_WORKSPACE_ID:process.env.JARVIS_DEV_WORKSPACE_ID,COMPANION_PAIRING_SECRET:process.env.COMPANION_PAIRING_SECRET});
